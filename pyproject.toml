[project]
name = "nudge-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["boto3>=1.34", "mcp>=1.0", "tzdata"]

[dependency-groups]
dev = ["pytest"]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
